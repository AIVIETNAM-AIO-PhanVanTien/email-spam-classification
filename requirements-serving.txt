# Serving-only dependencies for the EC2 deploy.
# No airflow, no dev/test tooling, no viz libs needed only by training.

# --- ML runtime (classifier + vectorizer + scaler) ---
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0
joblib>=1.3.0

# RandomForest / XGBoost runtime — winner can be any of LR/NB/RF/XGB
xgboost>=2.0.0

# --- Numeric feature computation (mirrors silver_transform) ---
pandas>=2.2.0

# --- Backend API ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0

# --- Frontend demo ---
streamlit>=1.32.0
requests>=2.31.0

# --- Gmail integration (opt-in; needed only when the poller runs) ---
google-api-python-client>=2.0.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
